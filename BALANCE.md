# 无穷道途（当前版）完整公式与平衡参数

本文档对应文件：`/Users/wangwenjie/Documents/code/open ai code playgroud/infinite_xianxia.py`。

## 1. 大数表示与比较

1. 数值状态：`(layer, mag)`
2. 解释：
- `layer=0`: `value=mag`
- `layer>0`: `value ~= 10^(10^(...^(mag)))`
3. 战斗比较分数：
- `score = layer * 2500 + mag`
4. 显示：
- `layer=1` -> `1e{mag}`
- `2<=layer<=7` -> `10^^layer(mag)`
- `8<=layer<=15` -> `高德纳[...]`
- `>=16` -> `葛立恒门[...]`

## 2. 成长公式（慢节奏 + 心魔约束）

记号：
- `g`：原始灵蕴
- `A`：增幅值
- `S`：增幅阶
- `P`：心魔压强

### 2.1 压强对成长/敌人的影响

- 玩家成长倍率：
`growth_factor = clamp(1 - max(0, P-35)*0.008, 0.52, 1.0)`
- 敌方伤害倍率：
`enemy_buff = clamp(1 + max(0, P-40)*0.012, 1.0, 1.95)`
- 生效灵蕴：
`final_essence = g * growth_factor`

### 2.2 增幅运算

- `scaled = max(0.01, final_essence) * max(0.15, A)`

按 `S`：
1. `S=0`：`stat += scaled`
2. `S=1`：`stat *= (1 + 0.18*scaled)`
3. `S=2`：`stat ^= (1 + 0.064*scaled*sqrt(max(0.5,A)))`
4. `S=3`：`n = 2 + floor((scaled+1)^0.42)`，`stat <- ↑ n`
5. `S=4`：`n = 2 + floor(log10(1+scaled)*2.0)`，`stat <- ↑↑ n`
6. `S>=5`：
- `arrows = 2 + (S-4)`
- `n = 2 + floor(log10(1+scaled)*(1+0.18*arrows))`
- `stat <- ↑^arrows n`

## 3. 战斗公式

### 3.1 单次伤害

- `pressure = clamp(att.score - def.score, -60, 60)`
- `swing = exp(pressure*0.055)`
- `guard = 1 + max(0, hp.score-att.score)*0.013`
- `variance ~ U(0.90, 1.10)`
- `raw = (6.2 + turn*0.68) * swing * variance / guard`
- `damage = clamp(round(raw * ratio), 1, 64)`

其中：
- 玩家 `ratio = growth_factor`
- 敌方 `ratio = enemy_buff`

### 3.2 心魔反噬

- 当 `P>=70`：每回合 22% 概率触发反噬
- 反噬伤害：3~8 专注（玩家）

### 3.3 战斗流程

- 双方专注 100
- 最多 30 回合
- 超时比较剩余专注

## 4. 新增核心玩法机制

## 4.1 宗门任务（中期目标）

每个境界有一组任务，必须完成后才能挑战守关者。

任务需求（按境界 idx）：
- `狩猎需求 = 3 + idx`
- `修炼需求 = 2 + floor(idx/2)`
- `调息需求 = 1 + floor(idx/3)`

任务完成奖励（一次性）：
- `修为 += hunt_exp * 1.8 * (1 + 0.10*cycle)`
- `灵蕴 += cultivate_essence * 1.35`
- `增幅值 += 0.03 + 0.01*idx`
- `心魔压强 -= 10`
- `寻宝令碎片 += 0.80 + 0.05*idx`

## 4.2 守关压制层（保底推进）

目标：减少“守关卡关纯看脸”。

- 每次挑战失败，目标守关者压制层 +1（上限5）
- 压制层会削弱该守关者：
- `suppress = 1 - 0.07*layer`
- 基础攻防血与成长幅度按 suppress 下调
- 成长步数：`steps = max(4, base_steps - layer)`

失败代价（当前值）：
- `修为 - break_need*0.12`
- `参悟回流 + break_need*0.05`
- 压强 `+16`

胜利：
- 获得破境印记
- 压制层清零

## 4.3 寻宝令限制

- `寻宝`消耗1令
- 令牌上限3
- 寻宝后冷却2回合

碎片来源：
- 修炼：`+0.38 + 0.03*realm_idx`
- 狩猎胜：`+0.20 + 0.04*rank_mod`
- 狩猎败：`+0.12`
- 调息：`+0.70 + 0.20*cycle`
- 守关胜：`+0.22`

## 5. 奖励与惩罚

### 5.1 狩猎

rank 修饰 `r`（普通0/精英1/首领2），轮回 `c`：
- `exp = hunt_exp * (1 + 0.42r) * (1 + 0.15c)`
- `essence = hunt_essence * (1 + 0.45r) * (1 + 0.10c)`
- `shardChance = clamp(0.02 + 0.03r + 0.008*realm_idx, 0.02, 0.50)`

压强变化：
- 胜：`+5.5 + 1.5*r`
- 败：`+10 + 2*r`

### 5.2 修炼

- `exp = cultivate_exp * (1 + 0.11*cycle) * U(0.88,1.10)`
- `essence = cultivate_essence * (1 + 0.08*realm_idx) * U(0.90,1.06)`
- 14% 顿悟：`amp += 0.04 + 0.015*realm_idx`
- 压强 `-8`

### 5.3 调息

- 压强 `-18`
- 寻宝令碎片 `+0.70 + 0.20*cycle`

### 5.4 寻宝

`q = random(0,1) + treasure_bias - 0.10*enemy_buff`

- `q < 0.80`：低收益补给
- `0.80~1.25`：普通遗宝
- `1.25~1.55`：守卫战
- `>=1.55`：神话遗宝

## 6. 守关与突破链路

1. 完成当前境界宗门任务
2. `challenge/守关`，击败守关者拿印记
3. `exp >= break_exp`
4. `break/突破`

突破后：
- `amp_stage = max(amp_stage, unlock_stage)`
- `amp_value += amp_bonus`
- 额外灵蕴成长：`cultivate_essence * 1.10`

## 7. 轮回与归零

上限：
- cycle0 -> idx3（金丹）
- cycle1 -> idx6（炼虚）
- cycle2+ -> idx9（无量）

触顶归零后：
- 境界/修为/守关印记/守关压制层重置
- 三维回到个位数
- 保留并强化增幅系统

归零奖励：

| 上限 idx | stage floor | stage + | amp 倍率 | legacy + |
| ---: | ---: | ---: | ---: | ---: |
| 3 | 2 | 1 | 1.16 | 1 |
| 6 | 3 | 1 | 1.22 | 2 |
| 9 | 5 | 1 | 1.30 | 3 |

## 8. 境界参数表

| idx | 境界 | 突破修为 | 狩猎修为 | 狩猎灵蕴 | 修炼修为 | 修炼灵蕴 | 怪物步数 | 解锁增幅阶 | 突破增幅奖励 | 寻宝偏置 |
| ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 凡躯 | 80 | 14 | 2.1 | 9 | 1.8 | 2 | 0 | 0.04 | 0.00 |
| 1 | 炼气 | 160 | 21 | 3.2 | 12 | 2.5 | 3 | 1 | 0.05 | 0.08 |
| 2 | 筑基 | 300 | 31 | 4.8 | 16 | 3.6 | 4 | 1 | 0.07 | 0.16 |
| 3 | 金丹 | 520 | 44 | 6.9 | 21 | 4.9 | 5 | 2 | 0.09 | 0.25 |
| 4 | 元婴 | 860 | 60 | 9.8 | 28 | 6.7 | 6 | 2 | 0.12 | 0.40 |
| 5 | 化神 | 1350 | 80 | 13.8 | 36 | 9.0 | 7 | 3 | 0.15 | 0.58 |
| 6 | 炼虚 | 2050 | 105 | 19.2 | 46 | 12.0 | 8 | 3 | 0.19 | 0.78 |
| 7 | 合体 | 3000 | 138 | 26.5 | 58 | 15.9 | 9 | 4 | 0.24 | 0.98 |
| 8 | 大乘 | 4300 | 182 | 36.0 | 72 | 20.8 | 10 | 4 | 0.31 | 1.20 |
| 9 | 无量 | 6000 | 240 | 49.0 | 88 | 27.0 | 11 | 5 | 0.40 | 1.45 |
